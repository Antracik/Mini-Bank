@model Mini_Bank.Models.RegistrantModel

@{
    ViewData["Title"] = "DetailsRegistrant";
}

<h1>Registrant Details</h1>

<div>
    <h4>Details for selected Registrant</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            <label asp-for="@Model.Id"></label>
        </dt>
        <dd class="col-sm-10">
            @Model.Id
        </dd>
        <dt class="col-sm-2">
            <label asp-for="@Model.FirstName"></label>
        </dt>
        <dd class="col-sm-10">
            @Model.FirstName
        </dd>
        <dt class="col-sm-2">
            <label asp-for="@Model.LastName"></label>
        </dt>
        <dd class="col-sm-10">
            @Model.LastName
        </dd>
        <dt class="col-sm-2">
            <label asp-for="@Model.Country"></label>
        </dt>
        <dd class="col-sm-10">
            @Model.Country
        </dd>
        <dt class="col-sm-2">
            <label asp-for="@Model.Address"></label>
        </dt>
        <dd class="col-sm-10">
            @Model.Address
        </dd>
    </dl>
</div>
<div>
    <a asp-controller="Registrant" asp-action="EditRegistrantView" asp-route-id="@Model.Id">Edit Registrant</a>
</div>
<div>
    <a asp-controller="Wallet" asp-action="CreateWalletView" asp-route-regId="@Model.Id">Create Wallet</a>
</div>

@if(Model.Wallets.Count > 0)
{
    <partial name="~/Views/Wallet/DisplayWallets.cshtml" model="Model.Wallets" />
}
else
{
    <h5>No wallets linked to user</h5>
}

<div>
    <a asp-action="DisplayRegistrants">Back to Registrant List</a>
</div>
